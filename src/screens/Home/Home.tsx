import { ScrollView, Text, View } from "react-native";

const HomeScreen = () => {

  return (
    <ScrollView>
      <View>
        <Text>Home Screen</Text>
      </View>
    </ScrollView>
  );
};

export default HomeScreen;
