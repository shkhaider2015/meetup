import { ScrollView, Text, View } from "react-native";

const AboutScreen = () => {

  return (
    <ScrollView>
      <View>
        <Text>About Screen</Text>
      </View>
    </ScrollView>
  );
};

export default AboutScreen;
